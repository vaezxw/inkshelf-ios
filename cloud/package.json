{
  "name": "inkshelf-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute inkshelf --remote --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute inkshelf --local --file=./schema.sql",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250911.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.34.0"
  }
}
